# only run when zsh is run as a login shell, e.g. when logging in on the console or via SSH
# won't be parsed by any other tools --> environment variables usually not available in an X11 session

source $HOME/.profile
